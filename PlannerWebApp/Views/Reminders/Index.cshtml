@model IEnumerable<ProjectsOnlyCRUDWithoutEntityTemplate.ViewModel.RemindersViewModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ReminderId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UserId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ReminderName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ReminderDescription)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ReminderId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UserId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ReminderName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ReminderDescription)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.ReminderId }) |
                @Html.ActionLink("Details", "Details", new { id = item.ReminderId }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.ReminderId })
            </td>
        </tr>
}
    </tbody>
</table>
